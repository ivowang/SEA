## Local Environment

- GPU: 8x A800 40GB (just use the local GPUs that are Idle)
- Conda already install. Create Python environment by your self and resolve the necessary dependencies.

## Network Proxy

Due to internet restrictions in mainland China, directly accessing websites like Google may get stuck. You can use the `clashon` command to enable a proxy and access these websites through the proxy.

## Git Usage

- Use `git` for version control and promptly synchronize with the remote GitHub repository (github.com/ivowang/SEA).
- The ssh public key of this machine has already been placed in my GitHub account (Github Username: ivowang).